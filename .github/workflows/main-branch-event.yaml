name: Push Events
on: 
  push:
    branches:
      - main
jobs:
  show-info:
      runs-on: ubuntu-latest
      steps:
      - name: info
        run: |
          echo "this is push event on a particular branch/branches"
